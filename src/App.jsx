import { Routes, Route } from 'react-router-dom'

// 引入组件
import Nav from './components/Nav'

const App = props => {
  return (
    <div>
      <Nav />
    </div>
  )
}

export default App
